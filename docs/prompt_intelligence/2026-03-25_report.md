# Prompt Intelligence Report

**Generated**: 2026-03-25
**Failure Patterns Detected**: 10
**Best Practices Identified**: 0

---


## Executive Summary

Analyzed trace logs across **6 workloads**.

### Impact Distribution
- 🔴 **BLOCKING**: 5 patterns (test gate failures, pipeline stops)
- 🟡 **DEGRADED**: 1 patterns (quality/performance issues)
- 🟢 **MINOR**: 4 patterns (cosmetic or one-off issues)

### Pattern Types
- **Unknown**: 4 patterns
- **Pii False Positive**: 3 patterns
- **Quality Threshold**: 2 patterns
- **Schema Error**: 1 patterns

### Top Blockers
1. **Cannot proceed without primary key** (1 occurrences across 1 workloads)
2. **Quality gate blocked due to completeness** (1 occurrences across 1 workloads)
3. **Test gate failed: missing primary key definition** (1 occurrences across 1 workloads)

### Estimated Time Savings
Addressing high-priority patterns could save **10-15 hours** across future onboardings.


## Failure Patterns

### HIGH PRIORITY: Blocking Issues
**Count**: 5 patterns

#### 1. Cannot proceed without primary key
### Pattern: Cannot proceed without primary key
**Type**: unknown
**Frequency**: 1 across 1 workloads
**Workloads**: test_customer_data
**Agent**: Metadata Agent (Phase 3)
**Impact**: BLOCKING
**Confidence**: 0.30

**Root Cause:**
Recurring pattern across 1 occurrences. Investigation needed.

**Recommendation:**
Review metadata agent logic for Cannot proceed without primary key. Add validation or adjust configuration in Phase 3.

**Examples:**

1. Workload: `test_customer_data`
   Error: `Cannot proceed without primary key`


#### 2. Quality gate blocked due to completeness
### Pattern: Quality gate blocked due to completeness
**Type**: quality_threshold
**Frequency**: 1 across 1 workloads
**Workloads**: test_marketing_data
**Agent**: Quality Agent (Phase 4)
**Impact**: BLOCKING
**Confidence**: 0.30

**Root Cause:**
Quality threshold set too strict for data characteristics. Need workload-specific tuning.

**Recommendation:**
Review quality threshold. Consider nullable columns and expected null patterns. Adjust threshold or exclude expected-null columns.

**Examples:**

1. Workload: `test_marketing_data`
   Error: `Quality gate blocked due to completeness`


#### 3. Test gate failed: missing primary key definition
### Pattern: Test gate failed: missing primary key definition
**Type**: unknown
**Frequency**: 1 across 1 workloads
**Workloads**: test_product_catalog
**Agent**: Metadata Agent (Phase 3)
**Impact**: BLOCKING
**Confidence**: 0.30

**Root Cause:**
Recurring pattern across 1 occurrences. Investigation needed.

**Recommendation:**
Review metadata agent logic for Test gate failed: missing primary key definition. Add validation or adjust configuration in Phase 3.

**Examples:**

1. Workload: `test_product_catalog`
   Error: `Test gate failed: missing primary key definition`


#### 4. Metadata validation failed - no PK specified
### Pattern: Metadata validation failed - no PK specified
**Type**: unknown
**Frequency**: 1 across 1 workloads
**Workloads**: test_order_history
**Agent**: Metadata Agent (Phase 3)
**Impact**: BLOCKING
**Confidence**: 0.30

**Root Cause:**
Recurring pattern across 1 occurrences. Investigation needed.

**Recommendation:**
Review metadata agent logic for Metadata validation failed - no PK specified. Add validation or adjust configuration in Phase 3.

**Examples:**

1. Workload: `test_order_history`
   Error: `Metadata validation failed - no PK specified`


#### 5. Completeness check failed for commission_rate
### Pattern: Completeness check failed for commission_rate
**Type**: unknown
**Frequency**: 1 across 1 workloads
**Workloads**: test_sales_data
**Agent**: Quality Agent (Phase 4)
**Impact**: BLOCKING
**Confidence**: 0.30

**Root Cause:**
Recurring pattern across 1 occurrences. Investigation needed.

**Recommendation:**
Review quality agent logic for Completeness check failed for commission_rate. Add validation or adjust configuration in Phase 4.

**Examples:**

1. Workload: `test_sales_data`
   Error: `Completeness check failed for commission_rate`



---

### MEDIUM PRIORITY: Degraded Performance
**Count**: 1 patterns

#### 1. KeyError: 'primary_key'
### Pattern: KeyError: 'primary_key'
**Type**: schema_error
**Frequency**: 3 across 3 workloads
**Workloads**: test_product_catalog, test_customer_data, test_order_history
**Agent**: Metadata Agent (Phase 3)
**Impact**: DEGRADED
**Confidence**: 0.70

**Root Cause:**
CSV sources lack explicit PK column. Agent infers from uniqueness but often wrong. User input missing during discovery phase.

**Recommendation:**
For CSV sources: ALWAYS ask 'What is the primary key?' before profiling. If composite key, list ALL columns.

**Prompt Section to Update**: `Phase 3: Metadata Agent`

**Suggested Patch:**
```markdown
⚠️ **CRITICAL: Primary Key Detection**

For CSV sources without explicit PK column:
1. ALWAYS ask user: "What is the primary key for this data?"
2. If composite key: List ALL columns in order
3. If no natural PK: Confirm use of row_hash
4. NEVER infer PK from uniqueness alone

Add this question to discovery checklist BEFORE profiling.
```

**Examples:**

1. Workload: `test_customer_data`
   Error: `KeyError: 'primary_key' in schema inference - no PK column found in CSV`

2. Workload: `test_product_catalog`
   Error: `KeyError: 'primary_key' - CSV source has no explicit primary key column`

3. Workload: `test_order_history`
   Error: `KeyError: 'primary_key' in metadata validation`



---

### LOW PRIORITY: Minor Issues
**Count**: 4 patterns

#### 1. Quality threshold: completeness
### Pattern: Quality threshold: completeness
**Type**: quality_threshold
**Frequency**: 2 across 2 workloads
**Workloads**: test_marketing_data, test_sales_data
**Agent**: Quality Agent (Phase 4)
**Impact**: MINOR
**Confidence**: 0.60

**Root Cause:**
Quality threshold set too strict for data characteristics. Need workload-specific tuning.

**Recommendation:**
Review quality threshold. Consider nullable columns and expected null patterns. Adjust threshold or exclude expected-null columns.

**Examples:**

1. Workload: `test_marketing_data`
   Error: `Quality threshold: completeness score 0.76 below threshold 0.80 for campaign_notes column`

2. Workload: `test_sales_data`
   Error: `Quality threshold: completeness score 0.72 below threshold 0.80 for commission_rate column`


#### 2. PII false positive: fund_name
### Pattern: PII false positive: fund_name
**Type**: pii_false_positive
**Frequency**: 1 across 1 workloads
**Workloads**: test_financial_data
**Agent**: Metadata Agent (Phase 3)
**Impact**: MINOR
**Confidence**: 0.30

**Root Cause:**
Name-based PII detection flagging non-PII columns. Need business-context aware exclusion rules.

**Recommendation:**
Add to PII detection exclusion list. Review name-based detection rules for business entities.

**Examples:**

1. Workload: `test_financial_data`
   Error: `PII detection error: fund_name flagged as PII (name-based detection)`


#### 3. Excessive PII masking affecting business logic
### Pattern: Excessive PII masking affecting business logic
**Type**: pii_false_positive
**Frequency**: 1 across 1 workloads
**Workloads**: test_financial_data
**Agent**: Metadata Agent (Phase 3)
**Impact**: MINOR
**Confidence**: 0.30

**Root Cause:**
Name-based PII detection flagging non-PII columns. Need business-context aware exclusion rules.

**Recommendation:**
Add to PII detection exclusion list. Review name-based detection rules for business entities.

**Examples:**

1. Workload: `test_financial_data`
   Error: `Excessive PII masking affecting business logic`


#### 4. PII false positive: product_name
### Pattern: PII false positive: product_name
**Type**: pii_false_positive
**Frequency**: 1 across 1 workloads
**Workloads**: test_financial_data
**Agent**: Metadata Agent (Phase 3)
**Impact**: MINOR
**Confidence**: 0.30

**Root Cause:**
Name-based PII detection flagging non-PII columns. Need business-context aware exclusion rules.

**Recommendation:**
Add to PII detection exclusion list. Review name-based detection rules for business entities.

**Examples:**

1. Workload: `test_financial_data`
   Error: `PII detection error: product_name flagged as PII`



## Best Practices

Not enough data to identify best practices yet. Keep onboarding workloads!

## Implementation Guide

### How to Apply These Findings

#### 1. Review High-Priority Patterns
Start with BLOCKING issues. These cause test gate failures and stop pipelines.

#### 2. Update Prompts
For each pattern with a `prompt_patch`:
1. Open the specified prompt file
2. Navigate to the specified section
3. Add the suggested patch text
4. Test with a new workload to verify the fix works

#### 3. Update Shared Utilities
Some patterns require code changes:
- PII false positives → Update `shared/utils/pii_detection_and_tagging.py`
- Schema validation → Update `shared/utils/schema_utils.py`
- Quality rules → Update `shared/utils/quality_checks.py`

#### 4. Document in CLAUDE.md
Once validated, add successful patterns to `CLAUDE.md`.

#### 5. Track Effectiveness
After applying fixes:
1. Run new onboardings
2. Check if the pattern stops appearing in trace logs
3. Update pattern confidence in future reports


---

## Next Steps

1. **Immediate**: Address all BLOCKING patterns
2. **Short-term**: Fix DEGRADED patterns
3. **Long-term**: Incorporate best practices into standard workflows

---

*Report generated by Prompt Intelligence MVP*
