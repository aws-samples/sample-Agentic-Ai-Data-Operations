[project]
name = "agentic-data-onboarding"
requires-python = ">=3.10"
dependencies = [
    "pytest",
    "pyyaml",
]

[project.optional-dependencies]
cedar = ["cedarpy"]
avp = ["boto3"]

[tool.pytest.ini_options]
addopts = "--import-mode=importlib"
testpaths = ["workloads", "shared"]
