source:
  name: fundamentals_sales
  description: "Reported sales/revenue fundamentals (annual + quarterly) by entity, fiscal period, and metric. Multi-metric ready."
  type: s3
  format: csv
  location:
    bucket: "${var:data_lake_bucket}"
    prefix: "raw/fundamentals/sales/"
  credentials:
    method: iam_role
    role_arn: "${secret:glue_service_role_arn}"
  schema:
    inferred: true
    crawler_database: fundamentals_sales_db
  frequency: ad_hoc
  partition_by: ingestion_date
  owner: fundamentals-data-team
  contact: fundamentals-data@company.com

ingestion:
  mode: batch
  schedule: null
  format_options:
    delimiter: ","
    header: true
    quote_char: '"'
    escape_char: "\\"
    null_values: ["", "NULL", "null", "N/A"]
  dedup_key: ["entity_id", "fiscal_period_id", "metric"]

  retention:
    policy: keep_all_versions
    s3_versioning: true
    lifecycle_expiration_days: null
